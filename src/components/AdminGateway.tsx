'use client';

/**
 * AdminGateway: Re-introduced to prevent build failure.
 * All admin logic has shifted to /admin/gate for manual verification.
 * This component remains as a no-oper to preserve layout integrity.
 */
export default function AdminGateway() {
    return null;
}
